import { CoverLayout } from '../components/CoverLayout';
import { HomeInfo } from '../constants/homeInfo';

function Home() {
  return (
    <CoverLayout info={HomeInfo} />
  );
}

export default Home;
